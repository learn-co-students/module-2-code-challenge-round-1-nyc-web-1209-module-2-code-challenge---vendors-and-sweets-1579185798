<%= form_for @vendorsweet do |f| %>
<%= f.collection_select :vendor_id, vendorsweet, :id, :name %> <br>
<%= f.collection_select :sweet_id, vendorsweet, :id, :name%> <br>
<%= f.number_field :integer %>
<%= f.submit "set vendorsweet" %>
<% end %>